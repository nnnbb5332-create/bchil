<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- أذونات الإنترنت -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <!-- أذونات الموقع -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_BACKGROUND_LOCATION" />

    <!-- أذونات الخدمة في الخلفية -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_LOCATION" />

    <!-- أذونات استخدام التطبيقات -->
    <uses-permission android:name="android.permission.PACKAGE_USAGE_STATS"
        tools:ignore="ProtectedPermissions" />

    <!-- أذونات الإشعارات -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <!-- ✅ إزالة package من هنا لأنه موجود في build.gradle كـ namespace -->
    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.ChildMonitor"
        android:usesCleartextTraffic="false">

        <!-- شاشة البداية -->
        <activity
            android:name=".activities.SplashActivity"
            android:exported="true"
            android:theme="@style/Theme.ChildMonitor.Splash">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- اختيار النمط -->
        <activity
            android:name=".activities.ModeSelectionActivity"
            android:exported="false" />

        <!-- تسجيل دخول الأب -->
        <activity
            android:name=".activities.ParentLoginActivity"
            android:exported="false"
            android:windowSoftInputMode="adjustResize" />

        <!-- تسجيل حساب الأب -->
        <activity
            android:name=".activities.ParentRegisterActivity"
            android:exported="false"
            android:windowSoftInputMode="adjustResize" />

        <!-- لوحة تحكم الأب -->
        <activity
            android:name=".activities.ParentDashboardActivity"
            android:exported="false" />

        <!-- تسجيل دخول الطفل -->
        <activity
            android:name=".activities.ChildLoginActivity"
            android:exported="false"
            android:windowSoftInputMode="adjustResize" />

        <!-- لوحة تحكم الطفل -->
        <activity
            android:name=".activities.ChildDashboardActivity"
            android:exported="false" />

        <!-- إضافة طفل -->
        <activity
            android:name=".activities.AddChildActivity"
            android:exported="false" />

        <!-- عرض موقع الطفل -->
        <activity
            android:name=".activities.ChildLocationActivity"
            android:exported="false" />

        <!-- خدمة الموقع -->
        <service
            android:name=".services.LocationService"
            android:enabled="true"
            android:exported="false"
            android:foregroundServiceType="location" />

        <!-- خدمة استخدام التطبيقات -->
        <service
            android:name=".services.AppUsageService"
            android:enabled="true"
            android:exported="false" />

    </application>
</manifest>
